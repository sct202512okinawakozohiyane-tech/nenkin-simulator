spring.application.name=PensionStartingSimulator

# 本番環境では classpath から静的ファイルを配信（file: パスは開発専用）
spring.web.resources.static-locations=classpath:/static/

# DevTools は本番では無効
spring.devtools.restart.exclude=
